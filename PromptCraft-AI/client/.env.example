# Backend API URL — used by the Vite dev proxy and production builds
# For local development, leave empty (defaults to http://localhost:5000)
# For production (Vercel), set this to your Render backend URL
VITE_API_URL=https://your-backend.onrender.com
